{
  "name": "claudecron",
  "version": "1.0.0",
  "description": "Local-first Claude prompt scheduler with a web UI",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "install-all": "npm install && cd client && npm install && npm run build && cd ..",
    "build": "cd client && npm run build && cd ..",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "better-sqlite3": "^9.2.2",
    "node-cron": "^3.0.2",
    "nodemailer": "^6.9.7",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
